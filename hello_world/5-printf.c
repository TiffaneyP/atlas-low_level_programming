#include <stdio.h>
/**
 * main -use printf to print an exact string, you jerk.
 *
 * Return: always return 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
