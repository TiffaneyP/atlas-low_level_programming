#include "main.h"
/**
 * _isupper- Checks to see if something is an uppercase char
 *@c: int input
 * Return: 1 if uppercase, 0 if otherwise.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
