#include "main.h"
/**
 * _abs - computes the abs val of an int.
 *@r: passed in integer
 *Return: Returned 0
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	else
	{
		return (r);
	}
return (0);
}
